# DISABLED - Hackathon is over, no need to keep Render alive every 5 min
# Re-enable if needed by uncommenting the 'on' section below

name: Keep Render Service Alive (DISABLED)

# on:
#   schedule:
#     - cron: '*/5 * * * *'
#   workflow_dispatch:

jobs:
  ping:
    if: false  # Never runs
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Endpoint
        run: |
          echo "Pinging ScamShield API at $(date)"
          curl -s -o /dev/null -w "%{http_code}" https://scamshield-honeypot.onrender.com/v1/health
